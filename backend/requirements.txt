# Install the CPU build of torch (skips the ~2GB CUDA download):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.141.1
uvicorn[standard]==0.52.4
sse-starlette==3.4.8
transformers==5.16.1
torch==2.13.0
numpy==2.5.2

# Pulled in transitively, pinned for reproducibility:
# pydantic, anyio, tokenizers, safetensors
